---
# EOS calamares paketinde initcpio/initcpiocfg modulleri yok; initramfs'i
# hedef sistemde bu adim uretir. HOOKS, /etc/mkinitcpio.conf.d/10-shalaos-plymouth.conf
# drop-in'inden gelir (cleanup archiso.conf'u ondan once siler).
dontChroot: false
timeout: 600
script:
- mkinitcpio -P
