# ShalaOS sürüm bilgisi (SemVer + kod adı).
# CI bu dosyayı `source` eder; VM'de elle derlerken de kaynak alınabilir:
#   set -a; . ./VERSION; set +a; ./build.sh
# Böylece profiledef.sh iso_version'ı SHALAOS_VERSION'dan alır (yoksa tarih bazlı).
SHALAOS_VERSION=1.0.4
SHALAOS_CODENAME=Dardania
